# Radekclicker 2 in SolidJS

See [SolidJS](https://www.solidjs.com/) intros and tutorials

## devel
`git clone`, `npm install`, `npm run dev`, localhost:3000

## prod
`npm run build`, the `dist/` directory
